---
name: CodeQL config
queries:
  # - name: Run custom queries
  #   uses: ./queries
  # - uses: security-experimental
  # - uses: security-extended
  - uses: security-and-quality
paths-ignore:
  - dist
